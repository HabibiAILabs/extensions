# Soul

Soul stores a user-authored agent personality prompt in the extension's private KV namespace and contributes it to every model invocation as extension-provided system context.

Open the extension from Habibi's Extensions page to edit the prompt. Saving an empty prompt disables the contribution. Prompts are bounded to 16 KiB.
