# Soul

Soul stores a user-authored agent personality prompt in the extension's private KV namespace and contributes it under a Markdown heading in every model invocation's system context.

Open the extension from Habibi's Extensions page to edit the prompt. Saving an empty prompt disables the contribution. Prompts are bounded to 16 KiB.
